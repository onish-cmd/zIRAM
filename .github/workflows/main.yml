name: zIRAM Kernel Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y clang llvm make linux-headers-$(uname -r)

    - name: Compile with GCC
      run: |
        make
        mv main.ko ziram_core.ko

    - name: Check Module Info
      run: modinfo ziram_core.ko

    - name: Load and Test Hijack
      run: |
        # 1. Force load kprobes and zram
        sudo modprobe zram
        
        # 2. Check if kprobes are actually supported by this kernel
        if [ ! -d /sys/kernel/debug/kprobes ]; then
          echo "ERROR: Kprobes not supported on this kernel!"
          exit 1
        fi

        # 3. Check if zram_bvec_write exists in the symbol table
        if ! sudo grep -q "zram_bvec_write" /proc/kallsyms; then
          echo "ERROR: Symbol zram_bvec_write not found. Try 'zram_write_page' instead."
          sudo grep "zram" /proc/kallsyms | head -n 10
          exit 1
        fi
        
        # 4. Try inserting your module now
        sudo insmod ziram_core.ko

    - name: Upload zIRAM Kernel Module
      uses: actions/upload-artifact@v4
      with:
        name: ziram-ko
        path: |
          ziram_core.ko
          Module.symvers
